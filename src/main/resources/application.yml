spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring_repository_1
    username: fconsiglio98
    password: inserirePassword
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false
